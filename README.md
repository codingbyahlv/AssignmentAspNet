In WebApi -> appsetting.json
Add "Secret" under "ApiKey"
Add "Secret" under "Token"

In WebApp -> appsettings.json
Add "AppId" and "AppSecret" under "FacebookAuth"
Add "ClientId" and "ClientSecret" under "GoogleAuth"
Add "ApiKey"
